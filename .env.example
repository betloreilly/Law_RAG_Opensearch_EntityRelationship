# OpenSearch — use one of the two styles below.
# Managed instance (e.g. IBM Cloud):
OPENSEARCH_URL=https://your-instance.example.com:9200
OPENSEARCH_USERNAME=your_username
OPENSEARCH_PASSWORD=your_password
# If you get SSL "wrong version number", the API may be HTTP on :9200. Set:
# OPENSEARCH_USE_HTTP=true
# (keeps your URL as https:// but connects with http:// for the client)
# Local / Docker (alternative names):
# OPENSEARCH_NODE=https://localhost:9200
# OPENSEARCH_USER=admin
# OPENSEARCH_PASSWORD=admin
# OPENSEARCH_INSECURE=true  # only for self-signed certs

# OpenAI for embeddings and chat (RAG)
OPENAI_API_KEY=sk-...
